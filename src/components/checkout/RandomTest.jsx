{
  /* <div className="flex items-center justify-center min-h-screen">
<div className="bg-green-600 text-white rounded-md shadow-md p-8">
  <h1 className="text-2xl font-bold mb-4">Payment Successful!</h1>
  <h2 className="text-xl mb-2">{selectedSuite}</h2>
  <ul className="list-disc list-inside mb-4">
    {selectedAddOns.map((addOn, index) => (
      <li key={index}>{addOn.name}</li>
    ))}
  </ul>
  {invoiceUrl && (
    <a
      href={invoiceUrl}
      target="_blank"
      rel="noopener noreferrer"
      className="bg-white text-green-600 px-4 py-2 rounded-md"
    >
      View your invoice
    </a>
  )}
</div>
</div> */
}

// const { selectedSuite, selectedAddOns } = useContext(PricingContext);
